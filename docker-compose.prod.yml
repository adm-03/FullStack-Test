version: '3.8'

services:
  backend:
    build:
      context: ./backend      
      dockerfile: Dockerfile
    ports:
      - "127.0.0.1:8000:8000" 
    restart: always
    
    env_file:
      - ./backend/.env